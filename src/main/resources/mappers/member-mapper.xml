<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="memberMapper">

	<select id="selectOne" parameterType="Member" resultType="Member">
  	
  		<!-- 암호화 후 로그인  -->
  		select * from member
  		where email=#{email}
  		and mstatus='Y'
  		
  	</select>
  	
  	<insert id="insertMember" parameterType="Member" >
  		insert into member 
  		values (member_seq.nextval, default, #{email}, #{pwd}, #{name}, #{nickname},
  				#{gender}, #{birth}, #{address}, #{phone}, default, sysdate, sysdate )
  	</insert>

	<update id="deleteMember" parameterType="Member">
		update Member
		set mstatus='N'
		where mNo=#{mNo}
	</update>
	
	<select id="checkIdDup" parameterType="string" resultType="_int">
		select count(*) from member
  		where email=#{email}
	</select>

</mapper>
